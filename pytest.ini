[pytest]
# Root pytest configuration for standalone daemon tests.
# Uses the repo root as rootdir and importlib import mode to avoid
# treating tests/ as a Python package (which would import tests/__init__.py
# requiring homeassistant — legacy HA code not needed for daemon tests).
addopts = -p no:cacheprovider --import-mode=importlib
testpaths = tests
